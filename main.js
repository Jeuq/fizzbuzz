

$main () {
    $('#number-choice').on('submit,'  function (fb(evt) {
      const input = $(evt.number-choice);
      console.log(input);
    });
  }

/*when submit is clicked
  take number entered
  check if divisible by 3 and 5
  check if divisible by 3
  check if divisible by 5
  otherwise print number
  assign styles to the above
  console.log .js-results

*/

$(main);